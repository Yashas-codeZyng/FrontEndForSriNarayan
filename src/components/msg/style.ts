import { styled, Box } from "@mui/material";

export const MessageContainerStyledBox = styled(Box)(() => ({
    width: "auto",
    maxWidth: "100%",
    paddingLeft: "10px",
    paddingRight: "10px",
    paddingTop: "10px",
    textAlign: "left",
}));